require 'spec_helper'

describe IRKit::Remote do
  it 'has a version number' do
    expect(IRKit::Remote::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
